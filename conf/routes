# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     / 					controllers.Application.index
#GET     /runningJobs 		controllers.Application.runningJobs
GET     /cancelJob/:id 		controllers.Application.cancelScheduledReport(id)
POST	/scheduleReport			controllers.Application.scheduleReport